# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

Initializing the backend...
╷
│ Error: validating provider credentials: retrieving caller identity from STS: operation error STS: GetCallerIdentity, https response error StatusCode: 403, RequestID: 6a0269ac-84bf-4cb9-9926-597ba903aaf6, api error InvalidClientTokenId: The security token included in the request is invalid.
│ 
│ 
╵

##[error]Bash exited with code '1'.

/usr/bin/bash --noprofile --norc /home/vsts/work/_temp/e4f90d2b-edae-4264-abae-c4b2d92fd3a0.sh
╷
│ Error: Backend initialization required, please run "terraform init"
│ 
│ Reason: Initial configuration of the requested backend "s3"
│ 
│ The "backend" is the interface that Terraform uses to store state,
│ perform operations, etc. If this message is showing up, it means that the
│ Terraform configuration you're using is using a custom configuration for
│ the Terraform backend.
│ 
│ Changes to backend configurations require reinitialization. This allows
│ Terraform to set up the new configuration, copy existing state, etc. Please
│ run
│ "terraform init" with either the "-reconfigure" or "-migrate-state" flags
│ to
│ use the current configuration.
│ 
│ If the change reason above is incorrect, please verify your configuration
│ hasn't changed and try again. At this point, no changes to your existing
│ configuration or state have been made.
╵

##[error]Bash exited with code '1'.


terraform plan
========================== Starting Command Output ===========================
/usr/bin/bash --noprofile --norc /home/vsts/work/_temp/e4f90d2b-edae-4264-abae-c4b2d92fd3a0.sh
╷
trigger:
  branches:
    include:
      - master

pr: none

parameters:
  - name: action
    displayName: "Type action: start | destroy"
    type: string
    default: start

pool:
  vmImage: ubuntu-latest

steps:

# =========================
# Checkout Code
# =========================
- checkout: self

# =========================
# Show Repo Structure
# =========================
- script: |
    ls -R
  displayName: "Show Repo Structure"

# =========================
# AWS Credentials
# =========================
- script: |
    export AWS_ACCESS_KEY_ID=$(AWS_ACCESS_KEY_ID)
    export AWS_SECRET_ACCESS_KEY=$(AWS_SECRET_ACCESS_KEY)
    export AWS_DEFAULT_REGION=ap-south-1
  displayName: "Set AWS Credentials"
  env:
    AWS_ACCESS_KEY_ID: $(AWS_ACCESS_KEY_ID)
    AWS_SECRET_ACCESS_KEY: $(AWS_SECRET_ACCESS_KEY)

# =========================
# Setup Terraform
# =========================
- task: TerraformInstaller@1
  displayName: "Install Terraform"
  inputs:
    terraformVersion: 'latest'

# =========================
# Clean Terraform Cache
# =========================
- script: |
    rm -rf .terraform
    rm -f .terraform.lock.hcl
  displayName: "Clean Terraform Cache"

# =========================
# Terraform Init
# =========================
- script: terraform init
  displayName: "Terraform Init"
  env:
    AWS_ACCESS_KEY_ID: $(AWS_ACCESS_KEY_ID)
    AWS_SECRET_ACCESS_KEY: $(AWS_SECRET_ACCESS_KEY)
    AWS_DEFAULT_REGION: ap-south-1

# =========================
# Terraform Validate
# =========================
- script: terraform validate
  displayName: "Terraform Validate"
  condition: eq('${{ parameters.action }}', 'start')
  env:
    AWS_ACCESS_KEY_ID: $(AWS_ACCESS_KEY_ID)
    AWS_SECRET_ACCESS_KEY: $(AWS_SECRET_ACCESS_KEY)
    AWS_DEFAULT_REGION: ap-south-1

# =========================
# Terraform Plan (START only)
# =========================
- script: terraform plan
  displayName: "Terraform Plan"
  condition: eq('${{ parameters.action }}', 'start')
  env:
    AWS_ACCESS_KEY_ID: $(AWS_ACCESS_KEY_ID)
    AWS_SECRET_ACCESS_KEY: $(AWS_SECRET_ACCESS_KEY)
    AWS_DEFAULT_REGION: ap-south-1

# =========================
# Terraform Apply (START)
# =========================
- script: terraform apply -auto-approve
  displayName: "Terraform Apply"
  condition: eq('${{ parameters.action }}', 'start')
  env:
    AWS_ACCESS_KEY_ID: $(AWS_ACCESS_KEY_ID)
    AWS_SECRET_ACCESS_KEY: $(AWS_SECRET_ACCESS_KEY)
    AWS_DEFAULT_REGION: ap-south-1

# =========================
# Debug State Info
# =========================
- script: |
    echo "Checking terraform files..."
    ls -R
    echo "Terraform state info"
    cat terraform.tfstate || echo "No local state file (using backend)"
  displayName: "Show State Debug Info"

# =========================
# Terraform Destroy
# =========================
- script: terraform destroy -auto-approve
  displayName: "Terraform Destroy"
  condition: eq('${{ parameters.action }}', 'destroy')
  env:
    AWS_ACCESS_KEY_ID: $(AWS_ACCESS_KEY_ID)
    AWS_SECRET_ACCESS_KEY: $(AWS_SECRET_ACCESS_KEY)
    AWS_DEFAULT_REGION: ap-south-1